{
  "name": "cad-vc-agent",
  "version": "1.0.0",
  "description": "Local agent — runs on workshop PC, bridges cloud portal to Bambu printer",
  "main": "agent.js",
  "scripts": {
    "start": "node agent.js"
  },
  "dependencies": {
    "basic-ftp": "^5.0.3",
    "dotenv": "^16.0.3",
    "mqtt": "^5.3.4",
    "socket.io-client": "^4.7.2"
  }
}
